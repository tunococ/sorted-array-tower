module;

#include <sorted_array_tower/merge.hpp>

/**
 * @brief Module interface for [merge.hpp](@ref merge.hpp).
 */
export module sorted_array_tower.merge;

export namespace sorted_array_tower {

using sorted_array_tower::merge;

}  // namespace sorted_array_tower
